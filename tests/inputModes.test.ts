import { describe, expect, it } from 'vitest';
import {
  CANONICAL_INPUT_MODES,
  formatInputModeLabel,
  isCanonicalInputMode,
  isStoredInputMode,
  normalizeInputMode,
} from '../src/core/adaptive/inputModes';

describe('adaptive input modes', () => {
});
