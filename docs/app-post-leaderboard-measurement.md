# App.tsx measurement after LeaderboardWorkspace

_Last measured: 2026-06-03_

This note captures the `src/App.tsx` size after the leaderboard workspace extraction pass and before the next adaptive workspace split was applied.

```text
9,370 lines
```

The next candidate recommended from that handoff was:

```text
src/components/adaptive-workspace/AdaptiveAdvancedDiagnostics.tsx
```

That candidate is now extracted in a later pass, so this document serves as the historical measurement checkpoint between the leaderboard and adaptive diagnostics modularization work.
