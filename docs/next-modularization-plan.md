# Next modularization pass

Created: 2026-06-10

## Context

This document supersedes the older "start with AdminWorkspace" recommendation for the current repository state. The current default branch is `product/input-2`, and the repository already contains completed modularization closeouts for several earlier `App.tsx` extractions.

Completed or closed-enough areas:

- `SessionDashboard` lives under `src/components/session-dashboard/`.
- `Adaptive