# Listening-First Product Language

User-facing training intents are `Precision`, `Stabilize`, and `Challenge`.

Historical values such as `easy`, `normal`, `hard`, and legacy express labels remain valid storage and routing values.

Do not migrate internal values just to change copy. Translate legacy metadata at presentation time.
